type TProperty = 'nickname' | 'tab' | 'session' | 'authorization';

type TTabs = 'Фильмы' | 'Телеканалы';

type TPositionBody = 'fixed' | 'static';

export {
	TProperty,
	TTabs,
	TPositionBody
};
